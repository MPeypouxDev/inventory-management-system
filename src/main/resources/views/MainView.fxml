<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.layout.BorderPane?>
<?import javafx.scene.layout.VBox?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>

<BorderPane xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.stockmanager.controller.MainController"
            prefWidth="1000" prefHeight="700">

    <!-- Barre du haut -->
    <top>
        <HBox style="-fx-background-color: #2c3e50; -fx-padding: 15px;" spacing="10">
            <Label text="Gestion de Stock"
                   style="-fx-text-fill: white; -fx-font-size: 18px; -fx-font-weight: bold;"/>
            <Label fx:id="alertLabel"
                   style="-fx-text-fill: #e74c3c; -fx-font-weight: bold; -fx-font-size: 14px;"/>
        </HBox>
    </top>

    <!-- Menu gauche -->
    <left>
        <VBox style="-fx-background-color: #34495e; -fx-padding: 10px;" spacing="5" prefWidth="200">
            <Button text="Produits" onAction="#showProducts"
                    prefWidth="180" style="-fx-background-color: transparent; -fx-text-fill: white;"/>
            <Button text="Fournisseurs" onAction="#showSuppliers"
                    prefWidth="180" style="-fx-background-color: transparent; -fx-text-fill: white;"/>
            <Button text="Mouvements" onAction="#showMovements"
                    prefWidth="180" style="-fx-background-color: transparent; -fx-text-fill: white;"/>
            <Button text="Déconnexion" onAction="#handleLogout"
                    prefWidth="180" style="-fx-background-color: transparent; -fx-text-fill: #e74c3c;"/>
        </VBox>
    </left>

    <!-- Zone centrale -->
    <center>
        <VBox fx:id="contentArea" style="-fx-padding: 20px;">
            <Label text="Bienvenue ! Sélectionnez une option dans le menu."
                   style="-fx-font-size: 16px;"/>
        </VBox>
    </center>

</BorderPane>